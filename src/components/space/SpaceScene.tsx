import OrbitalScene from './OrbitalScene';

/** @deprecated Use OrbitalScene — kept so older imports keep working. */
function SpaceScene() {
  return <OrbitalScene mode="space" />;
}

export default SpaceScene;
